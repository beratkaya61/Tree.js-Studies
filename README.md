# Tree.js-Studies

run server with "node server.js"
view your result on http://localhost:3000

# error and solving method

"access to XMLHttpRequest at 'file:///D:/models/truck.glb' from origin 'null' has been blocked by CORS policy: Cross origin requests are only supported for protocol schemes: chrome, chrome-extension, chrome-untrusted, data, http, https, isolated-app."

Option 1: Using http-server
    Install http-server globally using npm:
        npm install -g http-server
    Navigate to your project directory and start the server:
        http-server
